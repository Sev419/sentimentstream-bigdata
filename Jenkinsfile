pipeline {
    agent any

    stages {
        stage('Delegate') {
            steps {
                echo 'Use jenkins/Jenkinsfile as the documented CI pipeline for SentimentStream.'
            }
        }
    }
}
